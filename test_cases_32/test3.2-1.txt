=== 3.2 函数：有参有返回值 求绝对值 ===
// 测试函数定义、调用与参数传递
// abs(x)返回x的绝对值, main调用abs(-10)
int abs(int);

main(){
  int x = -10, y;
  y = abs(x);
  write(y);
}

int abs(int x) {
  if (x <= 0) return -x;
  return x;
}
=== 输入: (无) ===
=== 预期输出: 10 ===