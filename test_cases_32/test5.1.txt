=== 3.2 递归：双递归 斐波那契(紧凑写法) ===
// 测试双递归函数调用(嵌套表达式写法)
// 与test4.2-1不同: s=seq(m-1)+seq(m-2)直接用表达式
// 这种写法中间代码翻译正确但运行可能有问题, 需验证
// 输入: n, 范围1~15
int seq(int);
main()
{
   int n;
   n=read();
   write(seq(n));
}

int seq(int m) 
{
      int s,a,b;
      if(m<=2)
          s=1;
      else{
     s=seq(m-1)+seq(m-2);
      }
      return s;

}
=== 输入: 8 (1~15的整数) ===
=== 预期输出: 21 ===